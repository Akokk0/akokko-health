<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.akokko.dao.CheckgroupDao">
    <!-- 新增 -->
    <insert id="add" parameterType="com.akokko.pojo.CheckGroup">
        <selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="id">
            select LAST_INSERT_ID()
        </selectKey>
        insert into t_checkgroup(code,name,helpCode,sex,remark,attention)
        values
        (#{code},#{name},#{helpCode},#{sex},#{remark},#{attention})
    </insert>

    <insert id="connectionItemAndGroup" parameterType="map">
        insert into t_checkgroup_checkitem(t_checkgroup,t_checkitem)
        values
            (#{t_checkgroup},#{t_checkitem})
    </insert>
</mapper>